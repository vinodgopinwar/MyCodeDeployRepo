version: 0.0
os: linux
files:
   - source: /
     destination: /var/www/html
permissions:
   - object: /var/www/html
     pattern: "**"
     owner: ec2-user
     group: apache
     mode: 755
     type:
       - file
hooks:
  BeforeInstall:
    - location: /before-install.bat
      timeout: 900
